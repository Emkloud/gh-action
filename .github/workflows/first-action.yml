name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -  name: Print Greeting
         run: print("Hello World")
      -  name: Print Goodbye
         run: print("Done - Bye!")
